const local = 'http://192.168.1.58:3000';

const server = 'https://api.awlam.com';

const OneSignal_APP_Id = 'd92f7cdb-f715-430b-8fe0-3dc1b900f863';

const Google_Api_Key = 'AIzaSyCfF4LJJ3I5p_EdhV3O3FHk4nGej2UJheI';

const baseUrl = server;

const loginUrl = baseUrl + "/users/login";

const signupUrl = baseUrl + "/users/signup";

const articleUrl = baseUrl + "/articles";

const postUrl = baseUrl + "/posts";

const eventUrl = baseUrl + "/events";

const donationUrl = baseUrl + "/donations";

const citizenUrl = baseUrl + "/citizens";

const topicUrl = baseUrl + "/topics";

const governmentUrl = baseUrl + "/governments";

const userUrl = baseUrl + "/users";

const cmtUrl = baseUrl + '/comments';

export {
    baseUrl,
    loginUrl,
    signupUrl,
    articleUrl,
    postUrl,
    OneSignal_APP_Id,
    Google_Api_Key,
    eventUrl,
    cmtUrl,
    citizenUrl,
    topicUrl,
    governmentUrl,
    userUrl,
    donationUrl
}
